{
  "name": "flutter-dapp-provider",
  "version": "1.0.0",
  "dependencies": {
    "buffer": "^6.0.3",
    "eth-rpc-errors": "^4.0.3",
    "isutf8": "^4.0.0",
    "json-rpc-engine": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "typescript": "~4.7.0"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn.cmd run compile",
    "pretest": "yarn.cmd run compile",
    "posttest": "yarn.cmd run lint"
  }
}
